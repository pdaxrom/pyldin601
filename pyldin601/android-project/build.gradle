buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Downgraded the Android Gradle Plugin to a compatible version
        classpath 'com.android.tools.build:gradle:8.13.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

